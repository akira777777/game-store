# Local AI Agent - Обзор проекта

## 📦 Что это?

**Local AI Agent** — это автономный AI агент на базе моделей Qwen, работающий локально на вашем компьютере. Агент может:

- 💬 Общаться на естественном языке
- 🛠️ Выполнять команды (shell, git, python)
- 🧠 Запоминать контекст разговора
- 🌐 Предоставлять HTTP API для интеграции
- 🔒 Работать полностью офлайн (после загрузки модели)

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────┐
│              Local AI Agent                      │
├─────────────────────────────────────────────────┤
│                                                  │
│  ┌──────────────┐      ┌──────────────┐        │
│  │  Controller  │◄────►│     LLM      │        │
│  │              │      │ (ExLlamaV2)  │        │
│  └──────┬───────┘      └──────────────┘        │
│         │                                        │
│         ├───► ┌──────────────┐                 │
│         │     │    Memory    │                 │
│         │     │   (SQLite)   │                 │
│         │     └──────────────┘                 │
│         │                                        │
│         └───► ┌──────────────┐                 │
│               │    Tools     │                 │
│               │ (shell/git)  │                 │
│               └──────────────┘                 │
│                                                  │
└─────────────────────────────────────────────────┘
          ▲                        ▲
          │                        │
   ┌──────┴────────┐      ┌───────┴────────┐
   │ Interactive   │      │   HTTP API     │
   │    (CLI)      │      │  (FastAPI)     │
   └───────────────┘      └────────────────┘
```

## 📂 Структура проекта

```
local-agent/
├── agent/                    # Основной пакет агента
│   ├── __init__.py          # Package initialization
│   ├── controller.py        # Orchestration & workflow
│   ├── llm.py              # ExLlamaV2 wrapper
│   ├── memory.py           # Memory management
│   └── tools.py            # Tool execution
│
├── models/                  # Папка с моделями Qwen
│   └── qwen2.5-coder-7b-awq/  # Модель (загружается отдельно)
│
├── run.py                   # Интерактивный запуск
├── server.py               # FastAPI HTTP сервер
├── config.py               # Configuration management
├── download_model.py       # Скрипт загрузки моделей
│
├── requirements.txt        # Python зависимости
├── setup.ps1              # Автоматическая установка
│
├── README.md              # Полная документация
├── INSTALL.md             # Инструкции по установке
├── QUICKSTART.md          # Быстрый старт
│
├── .gitignore             # Git ignore
├── env.example            # Пример конфигурации
└── test_basic.py          # Базовые тесты
```

## 🚀 Быстрый старт

### 1. Установка (Windows)

```powershell
cd local-agent
.\setup.ps1
```

### 2. Загрузка модели

```powershell
# Рекомендуется для большинства систем (8GB VRAM)
python download_model.py --model qwen2.5-coder-7b-awq

# Легкая версия для слабых GPU (4GB VRAM)
python download_model.py --model qwen-1.8b
```

### 3. Запуск

```powershell
# Интерактивный режим
python run.py

# API сервер (http://localhost:8000)
python server.py
```

## 🔧 Компоненты

### 1. Controller (agent/controller.py)

**Роль**: Главный оркестратор агента

**Функции**:

- Обработка пользовательских запросов
- Координация между LLM, Memory и Tools
- Управление состоянием агента
- Формирование промптов с контекстом

**Состояния агента**:

- `IDLE` - ожидание запроса
- `THINKING` - обработка запроса через LLM
- `EXECUTING` - выполнение инструментов
- `COMPLETED` - задача выполнена
- `ERROR` - ошибка выполнения

### 2. LLM Wrapper (agent/llm.py)

**Роль**: Интеграция с моделями Qwen через ExLlamaV2

**Функции**:

- Загрузка и инициализация модели
- Генерация текста с параметрами (temperature, top-p)
- Парсинг tool calls из ответов модели
- Управление контекстом и историей

**Поддерживаемые модели**:

- Qwen 1.8B Chat
- Qwen 2.5 Coder 7B/14B (AWQ квантизация)
- Любые GGUF/AWQ модели, совместимые с ExLlamaV2

### 3. Memory Manager (agent/memory.py)

**Роль**: Управление краткосрочной и долгосрочной памятью

**Функции**:

- Хранение истории разговоров (SQLite)
- Поиск релевантного контекста
- Опциональный semantic search (ChromaDB)
- Автоматическая очистка старых записей

**База данных**:

```sql
CREATE TABLE memories (
    id INTEGER PRIMARY KEY,
    timestamp DATETIME,
    content TEXT,
    metadata TEXT,
    embedding TEXT
)
```

### 4. Tool Executor (agent/tools.py)

**Роль**: Выполнение системных команд и скриптов

**Доступные инструменты**:

| Tool | Описание | Пример |
|------|----------|--------|
| `shell` | Выполнение shell команд | `ls`, `mkdir`, `npm install` |
| `git` | Git операции | `git status`, `git commit` |
| `python` | Выполнение Python кода | `print("Hello")` |

**Безопасность**:

- Выполнение в изолированной среде
- Таймауты для предотвращения зависаний
- Валидация команд перед выполнением

## 🎯 Варианты использования

### 1. Интерактивная разработка

```
You: Create a new Python script that prints hello world
Agent: [создает hello.py с кодом]

You: Run the script
Agent: [выполняет: python hello.py]
Output: Hello, World!

You: Add error handling to the script
Agent: [обновляет код с try-except]
```

### 2. Автоматизация задач

```python
import requests

# Автоматический commit и push
requests.post("http://localhost:8000/query", json={
    "query": "Add all changes, commit with message 'Update', and push"
})

# Установка зависимостей
requests.post("http://localhost:8000/query", json={
    "query": "Install all packages from requirements.txt"
})
```

### 3. Анализ кодовой базы

```
You: Find all TODO comments in Python files
Agent: [выполняет grep/ripgrep и анализирует]

You: Show me the largest files in this project
Agent: [выполняет du -h и сортирует]
```

### 4. Интеграция с CI/CD

```yaml
# .github/workflows/agent.yml
- name: Run agent tasks
  run: |
    python -c "
    import requests
    requests.post('http://localhost:8000/query', json={
        'query': 'Run all tests and generate coverage report'
    })
    "
```

## ⚙️ Конфигурация

### Файл .env

```env
# Модель
MODEL_PATH=models/qwen2.5-coder-7b-awq
MAX_TOKENS=1000
TEMPERATURE=0.7

# Сервер
API_HOST=0.0.0.0
API_PORT=8000

# Память
MEMORY_DB_PATH=memory.db
VECTOR_STORE_PATH=vector_store
```

### Программная конфигурация

```python
from config import get_config

config = get_config()
print(config.model.path)  # models/qwen2.5-coder-7b-awq
print(config.server.port)  # 8000
```

## 🔌 API Endpoints

### POST /query

Отправить запрос агенту

**Request**:

```json
{
  "query": "List all Python files",
  "working_dir": ".",
  "model_path": "models/qwen2.5-coder-7b-awq"
}
```

**Response**:

```json
{
  "success": true,
  "message": "Query processed successfully",
  "result": "Found 10 Python files: ...",
  "tool_calls": [...]
}
```

### GET /state

Получить текущее состояние агента

### POST /clear_memory

Очистить старые записи в памяти

### GET /health

Health check

### GET /docs

Swagger UI документация

## 🧪 Тестирование

```powershell
# Запустить базовые тесты
python test_basic.py

# С pytest (если установлен)
pytest test_basic.py -v
```

## 📊 Системные требования

| Компонент | Минимум | Рекомендуется |
|-----------|---------|---------------|
| **Python** | 3.11+ | 3.11+ |
| **RAM** | 8GB | 16GB+ |
| **GPU VRAM** | 4GB (Qwen 1.8B) | 8GB+ (Qwen 7B AWQ) |
| **Disk** | 20GB | 50GB SSD |
| **OS** | Windows 10/11 | Windows 11 / Linux |

## 🔐 Безопасность

- ✅ Работает полностью локально (нет внешних API)
- ✅ Данные не покидают вашу систему
- ✅ Таймауты для предотвращения зависаний
- ⚠️ Агент может выполнять системные команды - используйте с осторожностью
- ⚠️ Не запускайте с повышенными привилегиями без необходимости

## 🛠️ Разработка

### Добавление нового инструмента

```python
# В agent/tools.py
async def _execute_my_tool(self, arguments: Dict[str, Any]) -> ToolResult:
    """Custom tool implementation"""
    # Your logic here
    return ToolResult(success=True, output="Result")

# Регистрация в __init__
self.tools["my_tool"] = self._execute_my_tool
```

### Расширение памяти

```python
# В agent/memory.py
async def add_structured_memory(self, data: Dict):
    """Add structured data to memory"""
    # Custom implementation
```

## 📚 Дополнительные ресурсы

- **ExLlamaV2**: <https://github.com/turboderp/exllamav2>
- **Qwen Models**: <https://huggingface.co/Qwen>
- **FastAPI**: <https://fastapi.tiangolo.com/>
- **ChromaDB**: <https://www.trychroma.com/>

## 🤝 Интеграция с проектами

### Node.js / TypeScript

```typescript
import axios from 'axios';

const agent = axios.create({
  baseURL: 'http://localhost:8000'
});

const response = await agent.post('/query', {
  query: 'Run npm test'
});
```

### Python

```python
import httpx

async with httpx.AsyncClient() as client:
    response = await client.post(
        "http://localhost:8000/query",
        json={"query": "Deploy to production"}
    )
```

### Cursor IDE (MCP)

Можно интегрировать через Model Context Protocol для использования в Cursor.

## 🐛 Troubleshooting

См. [INSTALL.md](local-agent/INSTALL.md) для детального решения проблем.

## 📝 Лицензия

MIT

---

**Создано для game-store проекта | Python 3.11 | Qwen 2.5 Coder**
