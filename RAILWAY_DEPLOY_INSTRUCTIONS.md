# 🚂 Railway Деплой - Пошаговая Инструкция

## ✅ Подготовка завершена

- ✅ Railway CLI установлен (v4.26.0)
- ✅ Node.js готов (v24.13.0)
- ✅ Middleware исправлен (API routes исключены)
- ✅ Код закоммичен в Git

---

## 🎯 Следующие шаги (выполните вручную)

### **ШАГ 1: Авторизация в Railway**

Откройте **новый PowerShell** в этой директории и выполните:

```powershell
cd "C:\Users\-\Desktop\game-store"
railway login
```

**Что произойдет:**

1. Откроется браузер
2. Появится страница Railway.app
3. Нажмите "Login with GitHub" или "Login with Google"
4. Авторизуйтесь
5. Вернитесь в терминал - увидите "Logged in as [ваш email]"

---

### **ШАГ 2: Создание проекта**

```powershell
railway init
```

**Выберите:**

- Create new project: **Yes**
- Project name: `game-store` (или любое имя)
- Starting template: **Empty Project**

---

### **ШАГ 3: Добавление PostgreSQL базы данных**

```powershell
railway add
```

**Выберите:**

- Service to add: **PostgreSQL**

Railway автоматически создаст базу и добавит `DATABASE_URL` в environment variables! 🎉

---

### **ШАГ 4: Настройка Environment Variables**

Откройте Railway Dashboard:

```powershell
railway open
```

**В браузере:**

1. Перейдите в ваш проект
2. Откройте сервис (не PostgreSQL, а ваш app)
3. Вкладка **Variables**
4. Добавьте:

```bash
# NextAuth (ОБЯЗАТЕЛЬНО!)
NEXTAUTH_SECRET=f8a7c3e9d2b1f4a6e8c7d9b3f2a5e1c4d6b8f7a9c3e5d2b4f6a8c1e3d5b7f9a2
NEXTAUTH_URL=${{RAILWAY_PUBLIC_DOMAIN}}

# Stripe (опционально, для payments)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# DATABASE_URL уже добавлен автоматически! ✅
```

**ВАЖНО:** `DATABASE_URL` автоматически создан Railway! Не трогайте его! ✅

---

### **ШАГ 5: Деплой!** 🚀

```powershell
railway up
```

**Что произойдет:**

1. ✓ Загрузка кода на Railway
2. ✓ Установка зависимостей (`npm install`)
3. ✓ Запуск Prisma migrations (`npx prisma generate`)
4. ✓ Build проекта (`npm run build`)
5. ✓ Запуск приложения (`npm start`)

**Время деплоя:** ~2-3 минуты ⏱️

---

### **ШАГ 6: Миграция базы данных**

После первого деплоя нужно запустить Prisma migrations:

```powershell
railway run npx prisma migrate deploy
```

Или:

```powershell
railway run npx prisma db push
```

---

### **ШАГ 7: Получить ссылку на сайт**

```powershell
railway domain
```

Или откройте dashboard:

```powershell
railway open
```

**Ваш сайт будет доступен по адресу:**
`https://game-store-production-xxxx.up.railway.app`

---

## 🎉 Готово

Ваш проект задеплоен на Railway! 🚀

---

## 📊 Полезные команды

```powershell
# Посмотреть логи
railway logs

# Открыть dashboard
railway open

# Добавить environment variable
railway variables set KEY=value

# Перезапустить приложение
railway restart

# Получить статус
railway status

# Добавить custom domain
railway domain
```

---

## ⚠️ Важные моменты

### 1. **NEXTAUTH_URL**

После первого деплоя обновите `NEXTAUTH_URL` на реальный домен:

```
NEXTAUTH_URL=https://game-store-production-xxxx.up.railway.app
```

### 2. **Database Migrations**

При первом деплое обязательно запустите:

```powershell
railway run npx prisma migrate deploy
```

### 3. **Build Settings (автоматически правильные)**

Railway автоматически определит:

- Build Command: `npm run build`
- Start Command: `npm start`
- Install Command: `npm install`

### 4. **Pricing**

- **$5/month** для hobby проектов
- **$500 бесплатных часов** при регистрации
- PostgreSQL база включена бесплатно в hobby план

---

## 🔥 Troubleshooting

### Проблема: Build fails

```powershell
railway logs --follow
```

Посмотрите логи и найдите ошибку.

### Проблема: Database connection error

Проверьте, что `DATABASE_URL` правильно настроен:

```powershell
railway variables
```

### Проблема: NextAuth ошибка

Убедитесь, что:

1. `NEXTAUTH_SECRET` добавлен
2. `NEXTAUTH_URL` указывает на ваш Railway домен

---

## 🎯 После успешного деплоя

1. ✅ Протестируйте сайт
2. ✅ Проверьте авторизацию
3. ✅ Добавьте несколько тестовых игр через админ панель
4. ✅ Обновите `NEXTAUTH_URL` на реальный домен
5. ✅ Настройте Stripe (если нужны payments)

---

## 📝 Альтернатива: Vercel (проще для начала)

Если Railway не получается, можно быстро задеплоить на Vercel:

```powershell
npm install -g vercel
vercel
```

**Плюсы Vercel:**

- ✅ Проще настройка (1 команда)
- ✅ Автоматический SSL
- ✅ GitHub integration из коробки
- ✅ Бесплатно для hobby проектов

**Минусы Vercel:**

- ⚠️ Serverless (холодные старты)
- ⚠️ База данных отдельно (Neon, Supabase)

---

## 🚀 Следующий шаг

После деплоя Game Store:

1. Скопируйте ссылку на сайт
2. Добавьте в портфолио
3. Запускайте Prague Client Finder Bot
4. Начинайте рассылку клиентам!

**Удачи с деплоем!** 💪

---

*Инструкция создана: 19 января 2026*
*Проект: Game Store Marketplace*
*Разработчик: Artem Mikhailov*
