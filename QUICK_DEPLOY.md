# Быстрое развертывание на Vercel

## ⚡ Самый быстрый способ получить публичную ссылку

### Шаг 1: Подготовка (2 минуты)

1. Убедитесь, что проект закоммичен в Git:
```bash
cd c:\Users\-\Desktop\game-store
git add .
git commit -m "Prepare for deployment"
```

2. Если у вас нет репозитория на GitHub:
   - Создайте репозиторий на [github.com](https://github.com)
   - Загрузите код туда

### Шаг 2: Регистрация на Vercel (3 минуты)

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите **Sign Up** и зарегистрируйтесь через GitHub
3. Авторизуйте доступ к вашему репозиторию

### Шаг 3: Развертывание (5 минут)

1. На странице Vercel нажмите **Add New...** → **Project**
2. Выберите репозиторий `game-store`
3. Нажмите **Import**

Vercel автоматически определит Next.js и начнет сборку.

### Шаг 4: Настройка базы данных (10 минут)

⚠️ **Важно:** Сейчас проект использует SQLite, который не подходит для продакшена. Нужно переключиться на PostgreSQL.

**Простой вариант - Vercel Postgres:**

1. В Vercel Dashboard откройте **Storage**
2. Создайте **Postgres** базу данных (бесплатно)
3. Скопируйте `DATABASE_URL` из настроек базы данных

**Затем обновите схему Prisma:**

1. Откройте `prisma/schema.prisma`
2. Измените строку 9:
```prisma
provider = "postgresql"  // было: "sqlite"
```

3. Примените миграцию к новой базе данных (локально с новым DATABASE_URL в .env):
```bash
npm run db:migrate
npm run db:generate
```

### Шаг 5: Настройка переменных окружения (5 минут)

В Vercel Dashboard → Settings → Environment Variables добавьте:

```
DATABASE_URL=<из Vercel Postgres>
NEXTAUTH_SECRET=<сгенерируйте: openssl rand -base64 32>
NEXTAUTH_URL=https://your-project.vercel.app
```

После добавления перейдите в **Deployments** и нажмите **Redeploy** на последнем деплое.

### Шаг 6: Готово! 🎉

Ваша публичная ссылка:
```
https://your-project.vercel.app
```

Поделитесь этой ссылкой для оценки проекта!

---

## Альтернативный вариант: Развертывание без изменений (для быстрого демо)

Если нужно просто показать интерфейс без полной функциональности БД:

1. Используйте [Turso](https://turso.tech) для SQLite в облаке (бесплатно)
2. Или используйте [Neon](https://neon.tech) для PostgreSQL (бесплатно)

Это займет дополнительно 5-10 минут настройки.

---

## Нужна помощь?

Полная инструкция доступна в файле `DEPLOY.md`
