# Quick Start Guide

## 🚀 Запуск за 5 минут

### 1. Автоматическая установка (рекомендуется)

```powershell
cd local-agent
.\setup.ps1
```

Этот скрипт:
- ✅ Проверит Python 3.11
- ✅ Установит все зависимости
- ✅ Настроит окружение

### 2. Загрузка модели

Выберите модель в зависимости от вашего GPU:

```powershell
# Для GPU с 4GB VRAM
python download_model.py --model qwen-1.8b

# Для GPU с 8GB+ VRAM (рекомендуется)
python download_model.py --model qwen2.5-coder-7b-awq

# Для GPU с 16GB+ VRAM
python download_model.py --model qwen2.5-coder-14b-awq
```

**Примечание**: Загрузка может занять 10-30 минут в зависимости от скорости интернета.

### 3. Запуск агента

#### Интерактивный режим:
```powershell
python run.py
```

#### API Сервер:
```powershell
python server.py
```

Откройте `http://localhost:8000/docs` для API документации.

## 📋 Примеры использования

### Интерактивный режим

```
You: List all Python files in the current directory
Agent: [выполняет команду ls *.py]

You: Create a new file hello.py with a simple print statement
Agent: [создает файл с кодом]

You: What files did we create?
Agent: [использует память и отвечает]
```

### API запросы (curl)

```bash
# Отправить запрос
curl -X POST http://localhost:8000/query \
  -H "Content-Type: application/json" \
  -d '{"query": "List files in current directory"}'

# Проверить состояние
curl http://localhost:8000/state

# Health check
curl http://localhost:8000/health
```

### API запросы (Python)

```python
import requests

# Отправить запрос агенту
response = requests.post(
    "http://localhost:8000/query",
    json={"query": "List all Python files"}
)
print(response.json())
```

## ⚙️ Настройка (опционально)

Создайте файл `.env` для настройки:

```bash
# Скопируйте пример
cp env.example .env

# Отредактируйте .env
notepad .env
```

Основные параметры:
- `MODEL_PATH` - путь к модели
- `TEMPERATURE` - креативность (0.0-1.0)
- `MAX_TOKENS` - максимум токенов

## 🔧 Решение проблем

### Python 3.11 не найден
```powershell
py --list  # Проверить установленные версии
```
Скачайте с [python.org](https://www.python.org/downloads/)

### Модель не загружается
```powershell
# Проверить наличие модели
ls models\
```

### Out of Memory
Используйте модель меньшего размера:
```powershell
python download_model.py --model qwen-1.8b
```

### Медленная генерация
- Убедитесь, что используется GPU: `nvidia-smi`
- Используйте AWQ квантизованные модели
- Уменьшите `MAX_TOKENS` в `.env`

## 📚 Дополнительные ресурсы

- **Полная документация**: [README.md](README.md)
- **Установка**: [INSTALL.md](INSTALL.md)
- **Поддержка**: Создайте issue в репозитории

## 🎯 Следующие шаги

1. Изучите примеры в `README.md`
2. Попробуйте разные модели Qwen
3. Настройте параметры в `.env`
4. Интегрируйте с вашим проектом через API

---

**Нужна помощь?** Проверьте [INSTALL.md](INSTALL.md) для детальных инструкций.
